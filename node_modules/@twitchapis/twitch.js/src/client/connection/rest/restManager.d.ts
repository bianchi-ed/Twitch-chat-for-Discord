import { Client } from '../../client';
/**
 * @private
 */
export declare class RestManager {
    private options;
    private methodsPath;
    private methods;
    private client;
    constructor(client: Client);
    /**
     * @private
     */
    loadAllMethods(): Promise<void>;
    private loadMethodsFolder;
    private loadMethods;
    get(method: string, ...params: any): Promise<any>;
    post(method: string, params: any): Promise<any>;
    put(method: string, params: any): Promise<any>;
    delete(method: string, params: any): Promise<any>;
}
//# sourceMappingURL=restManager.d.ts.map