import { ILoggerOptions, ELoggerLevel } from './interfaces';
export declare class Logger {
    private defaultLevel;
    private debugActive;
    private prefix?;
    private coloredBackground;
    private disableFatalCrash;
    private allLineColored;
    private fileProperties;
    private latestFileStream?;
    private debugLogStream?;
    private errorLogStream?;
    private htmlBackgroundColor;
    private htmlTextColor;
    private defaultHeader;
    constructor({ prefix, debug, defaultLevel, coloredBackground, disableFatalCrash, allLineColored, fileProperties }: ILoggerOptions);
    private closeFileStreams;
    private writeToAllStreams;
    private compressLastSessionLogs;
    private getFormattedPrefix;
    private getTime;
    private generateMessagePrefix;
    log(text: any, levelToLog?: ELoggerLevel, ...args: any): void;
    info(text: any, ...args: any): void;
    warn(text: any, ...args: any): void;
    error(text: any, ...args: any): void;
    debug(text: any, ...args: any): void;
    fatal(text: any, ...args: any): void;
}
//# sourceMappingURL=logger.d.ts.map